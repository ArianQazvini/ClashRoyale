package model.informations;

import enums.Level;

/**
 * The type Level value.
 */
public class LevelValue extends LevelInformation {
    /**
     * Instantiates a new Level value.
     *
     * @param level the level
     */
    public LevelValue(Level level){
        super(level);
    }
}
